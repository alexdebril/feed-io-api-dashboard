export class Item {
    constructor(
      public title: string,
      public url: string,
      public feedName: string,
      public feedUrl: string,
      public lastModified: string
    ) {
    }
}
